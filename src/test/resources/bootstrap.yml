#Spring Configuration
spring:
  application:
    name: screening-admin-service
  cloud:
    config:
      enabled: false
# H2
  jpa:
    hibernate:
      ddl-auto: create-drop
eureka:
  client:
     enabled: false
     
server:
  port: 8182