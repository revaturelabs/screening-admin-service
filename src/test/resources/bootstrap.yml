#Spring Configuration
spring:
  application:
    name: screening-admin-service
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
eureka:
  client:
    enabled: false