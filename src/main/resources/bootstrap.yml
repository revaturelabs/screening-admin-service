#Spring Configuration
spring:
  application:
    name: screening-admin-service
  cloud:
   config:
     uri: http://ec2-18-216-132-100.us-east-2.compute.amazonaws.com:8888/
     fail-fast: true   
  #Configure Test H2 Database
  datasource:
    driver-class-name: org.h2.Driver
    platform: h2
    url: jdbc:h2:mem:data;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none